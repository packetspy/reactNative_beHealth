import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    boxResult: {
        width: '100%',
        flex: 1,
        marginTop: 15,
        paddingTop: 30,
        borderRadius: 30,
        alignItems: 'center',

    },

    textMessage: {
        fontSize: 18,
        color: '#ff0043',
        fontWeight: 'bold',
    },

    textResult: {
        fontSize: 48,
        color: '#ff0043',
        fontWeight: 'bold',
    },
})

export default styles
